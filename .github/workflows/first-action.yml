name: First Workflow
on: workflow_dispatch
jobs:
    first-job:
      runs-on: windows-latest
      steps:
        - name: Print greetings
          run: echo "Hello World- Kazi"
        - name: Print Goodbye
          run: echo "Done -bye!"
          
          
